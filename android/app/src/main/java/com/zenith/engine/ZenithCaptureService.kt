package com.zenith.engine

/**
 * ZenithCaptureService: Production alias for [ScreenCaptureService].
 * Implements Foreground Service with MediaProjection for low-latency live streaming,
 * dynamic 1080p resolution clamping, and autonomous spatial co-pilot reasoning.
 */
class ZenithCaptureService : ScreenCaptureService()
